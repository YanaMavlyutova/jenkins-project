#!groovy
properties([disableConcurrentBuilds()])

pipeline {
    agent {
      lable 'main'
      }
    options {
      timestamps()
  }
  stages {
    stage("First step") {
      steps {
        sh '\'hosthame\''
      }
    }
  }
}
